# Fully-Functional-To-Do-List
This project is a fully coded interactive to-do list application built with HTML, CSS, and JavaScript. Starting from a static layout, the goal was to turn a simple design into a structured, dynamic task-management interface where users can add teammates, assign tasks with due dates, track progress, and preserve their work between sessions.
